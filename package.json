{
    "name": "web-dev-final-jose",
    "version": "0.0.0",
    "private": true,
    "scripts": {
        "build": "cd frontend && npm i && npm run build",
        "start": "cd backend && npm i && npm start"
    },
    "dependencies": {
        "prop-types": "^15.7.2"
    }
}
